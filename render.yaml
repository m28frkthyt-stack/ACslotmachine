services:
  - type: web
    name: inverse-cancel-casino
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run accasino.py --server.port $PORT --server.address 0.0.0.0
